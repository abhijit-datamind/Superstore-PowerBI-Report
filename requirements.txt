powerbiclient>=0.2.0
pandas>=2.0.0
matplotlib>=3.5.0
seaborn>=0.0.12
numpy>=1.23.0

1. Open Power BI Desktop.
2. Go to File > Options and settings > Python scripting, and set the Python environment to the path to your Python environment (venv).
3. Load superstore.csv into Power BI (Get > Data > Text/CSV).